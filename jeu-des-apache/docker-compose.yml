services:
  frontend:
    build: ./frontend
    container_name: jeu_frontend_apache
    ports:
      - "8081:80"
    depends_on:
      - backend

  backend:
    build: ./backend
    container_name: jeu_backend_multinode
    ports:
      - "8080:8080"
